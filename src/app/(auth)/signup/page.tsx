import { SignupForm } from '@/components/auth/signup-form'

export const metadata = {
  title: 'Sign Up | Relaks',
  description: 'Create your Relaks account',
}

export default function SignupPage() {
  return <SignupForm />
}
